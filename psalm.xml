<?xml version="1.0"?>
<psalm
    errorLevel="4"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    phpVersion="8.4"
>
    <projectFiles>
        <directory name="src/App"/>
        <directory name="src/Core"/>
        <ignoreFiles>
            <directory name="vendor"/>
            <directory name="src/views"/>
            <directory name="src/routes"/>
            <directory name="src/config"/>
            <directory name="src/database"/>
        </ignoreFiles>
    </projectFiles>

    <issueHandlers>
        <!-- Superglobais são usadas intencionalmente no Core -->
        <UnusedParam errorLevel="suppress"/>
    </issueHandlers>
</psalm>
